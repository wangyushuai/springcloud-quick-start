spring:
  cloud:
    consul:
      host: localhost  #阿里云测试Consul
      port: 8500
      discovery:
        instance-id: ${spring.application.name}-${random.uuid}-${spring.cloud.consul.port}
        service-name: ${spring.application.name}
#      config:
#        name: ${spring.application.name}
#        enabled: true
#        format: yaml
#        watch:
#          enabled: true
#        fail-fast: false
#        prefix: configuration
#        default-context: container
#        data-key: app



